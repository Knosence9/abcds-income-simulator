<footer class="site-footer">
  <div>
    <strong>ABCDs Income Simulator</strong>
    <p>Educational planning tools—not financial advice. Review the assumptions, risks, and methodology before making decisions.</p>
    <p>Your planner inputs and imported budget snapshots stay in your browser's local storage unless you choose to export them.</p>
  </div>
  <nav aria-label="Footer navigation">
    <a href="/getting-started">Getting started</a>
    <a href="/budget">Weekly budget</a>
    <a href="/simulator">Simulator</a>
    <a href="/#rules">Methodology &amp; rules</a>
  </nav>
</footer>

<style>
  .site-footer {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 24px;
    margin-top: 28px;
    border: 1px solid var(--line, rgba(23, 36, 58, .15));
    border-radius: 24px;
    padding: 22px;
    background: rgba(255, 253, 247, .82);
    color: var(--ink, #17243a);
    box-shadow: 0 14px 38px rgba(42, 31, 13, .09);
  }
  .site-footer > div { max-width: 700px; }
  .site-footer strong { color: var(--a, #203f69); }
  .site-footer p {
    margin: 6px 0 0;
    color: var(--muted, #617086);
    font-size: 13px;
    line-height: 1.5;
  }
  .site-footer nav {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 8px;
  }
  .site-footer a {
    min-height: 42px;
    display: inline-flex;
    align-items: center;
    border: 1px solid var(--line, rgba(23, 36, 58, .15));
    border-radius: 999px;
    padding: 0 13px;
    background: rgba(255, 255, 255, .56);
    color: var(--a, #203f69);
    font-weight: 850;
    text-decoration: none;
  }
  .site-footer a:hover { background: rgba(32, 63, 105, .08); }
  .site-footer a:focus-visible {
    outline: 3px solid var(--b, #bf7327);
    outline-offset: 3px;
  }
  @media (max-width: 760px) {
    .site-footer { flex-direction: column; }
    .site-footer nav { justify-content: flex-start; }
  }
</style>
